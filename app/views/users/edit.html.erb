<h2>ユーザー設定変更</h2>

<%= render 'form' %>

<%= link_to 'ユーザ詳細に戻る', user_path(@user.id), class: 'btn btn-primary' %>
<%= link_to '退会する', user_path(@user.id), class: 'btn btn-danger', method: :delete, data: { confirm: '本当に退会しますか？'} %>
